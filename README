Set Up Git
https://help.github.com/articles/set-up-git

SSH Key 생성
~$ ssh-keygen -t rsa
끝날때까지 엔터

~$ cat ~/.ssh/id_rsa.pub
이후에 출력되는 문자들을 전부 복사

SSH Key 등록
본인 계정의 https://github.com/settings/
복사한 SSH Key를 등록.

